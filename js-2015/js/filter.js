
export default (data) => data.data.children
	.filter(post => !post.data.over_18)
	.map(post => post.data.url)


if (this) {
	console.log('hi')
	if (this && window) {
		console.log('yeah')
		console.log('yeah')
	}
}

function fetchit() {
	fetch();
}


var regexp = '.gif';
filter(url => url.match(regexp))
	.map(url => url.replace('.gifv', '.gif'))
	.map(url => url.replace('http:', 'https:'));
	.map(url => url.replace('http:', 'https:'));
	.map(url => url.replace('http:', 'https:'));